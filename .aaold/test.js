function createDiv({nom, classNames, id, attribut, style}) {
	console.log(nom, classNames,id, attribut)
}



createDiv({
    nom : "Pong",
    classNames : ["glass", "pongGame"], 
    id : "test",
    }
)
